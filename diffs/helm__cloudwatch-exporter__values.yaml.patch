diff --git a/vendor/cloudwatch-exporter/values.yaml b/helm/cloudwatch-exporter/values.yaml
index 17d7b3a..f3638c3 100644
--- a/vendor/cloudwatch-exporter/values.yaml
+++ b/helm/cloudwatch-exporter/values.yaml
@@ -5,8 +5,10 @@
 replicaCount: 1
 
 image:
-  registry: quay.io
-  repository: prometheuscommunity/yet-another-cloudwatch-exporter
+  # Giant Swarm mirror (retagged into the giantswarm/ namespace) of
+  # quay.io/prometheuscommunity/yet-another-cloudwatch-exporter.
+  registry: gsoci.azurecr.io
+  repository: giantswarm/yet-another-cloudwatch-exporter
   pullPolicy: IfNotPresent
   # Overrides the image tag whose default is the chart appVersion.
   tag: ""
@@ -151,7 +153,7 @@ aws:
 
 serviceMonitor:
   # When set true then use a ServiceMonitor to configure scraping
-  enabled: false
+  enabled: true
   # Set the namespace the ServiceMonitor should be deployed
   # namespace: monitoring
   # Set how frequently Prometheus should scrape
